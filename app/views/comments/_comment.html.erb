<%= div_for(comment) do %>
	<div class="comments_wrapper clearfix">
		<div class="pull-left">

			<%= image_tag gravatar_url(comment.user.email, 50), class:"round-img" %>
			<p class="lead"><%= comment.body %></p>
			<p><small><b>Enviado por:</b> <%= comment.user.email %></small></p>
			<% if comment.user == current_user %>
            <%= link_to 'Destruir', comment, method: :delete, data: { confirm: 'Você tem certeza?' }, class: "btn btn-sm btn-default" %>
			<% end %>
		</div>
	</div>
<% end %>